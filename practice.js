const practiceCopy = {
  uk: {
    navFinder:"Пошук університетів", navPractice:"Практика", navRoi:"Калькулятор ROI", back:"До каталогу", eyebrow:"FULLRIDE / PRACTICE", title:"Вчіть слова<br /><em>у своєму темпі.</em>", intro:"Додавайте незнайомі англійські слова, зберігайте їх і повертайтеся до них у коротких випадкових сесіях.", saved:"карток збережено", learned:"вивчено", addLabel:"НОВА КАРТКА", addTitle:"Додайте слово для практики", wordLabel:"Англійське слово", translationLabel:"Переклад", optional:"(можна змінити)", addButton:"Додати картку", translationNote:"Переклад запитується автоматично. Перед збереженням його можна відредагувати.", libraryLabel:"МОЇ КАРТКИ", libraryTitle:"Повторюйте те, що важливо", startButton:"Почати практику", searchPlaceholder:"Пошук за словами і перекладами", filterAll:"Усі картки", filterLearning:"У процесі", filterLearned:"Вивчені", sessionLabel:"СЕСІЯ", sessionTitle:"Згадайте переклад", revealButton:"Показати переклад", repeatButton:"Повторити", knowButton:"Знаю", nextButton:"Далі", sentenceLabel:"Ваше речення", sentencePlaceholder:"Складіть англійське речення з цим словом", sentenceHint:"Напишіть повне речення англійською, щоб перейти далі.", checkSentence:"Перевірити речення", checkingSentence:"Перевіряємо граматику…", sentenceCorrect:"Граматика й пунктуація коректні.", sentenceError:"У реченні є помилки.", sentenceNeedWord:"Використайте слово з картки у реченні.", sentenceServiceError:"Сервіс перевірки тимчасово недоступний. Спробуйте ще раз.", correctedLabel:"Можливий виправлений варіант", sentenceTranslation:"Переклад речення", empty:"Поки немає карток. Додайте перше англійське слово.", noMatch:"За цим запитом карток не знайдено.", added:"Картку додано.", deleted:"Картку видалено.", autoError:"Не вдалося отримати переклад автоматично. Вкажіть переклад вручну.", translating:"Шукаємо переклад…", duplicate:"Це слово вже є у ваших картках.", footerTagline:"Практика маленькими кроками.", deleteAria:"Видалити картку", learnedAria:"Позначити як вивчену", learningAria:"Повернути до вивчення", count:"карток", sessionDone:"Сесію завершено. Чудова робота!"
  },
  ru: {
    navFinder:"Поиск университетов", navPractice:"Практика", navRoi:"Калькулятор ROI", back:"К каталогу", eyebrow:"FULLRIDE / PRACTICE", title:"Учите слова<br /><em>в своём темпе.</em>", intro:"Добавляйте незнакомые английские слова, сохраняйте их и возвращайтесь к ним в коротких случайных сессиях.", saved:"карточек сохранено", learned:"выучено", addLabel:"НОВАЯ КАРТОЧКА", addTitle:"Добавьте слово для практики", wordLabel:"Английское слово", translationLabel:"Перевод", optional:"(можно изменить)", addButton:"Добавить карточку", translationNote:"Перевод запрашивается автоматически. Перед сохранением его можно отредактировать.", libraryLabel:"МОИ КАРТОЧКИ", libraryTitle:"Повторяйте то, что важно", startButton:"Начать практику", searchPlaceholder:"Поиск по словам и переводам", filterAll:"Все карточки", filterLearning:"В процессе", filterLearned:"Выученные", sessionLabel:"СЕССИЯ", sessionTitle:"Вспомните перевод", revealButton:"Показать перевод", repeatButton:"Повторить", knowButton:"Знаю", nextButton:"Дальше", sentenceLabel:"Ваше предложение", sentencePlaceholder:"Составьте английское предложение с этим словом", sentenceHint:"Напишите полное предложение на английском, чтобы перейти дальше.", checkSentence:"Проверить предложение", checkingSentence:"Проверяем грамматику…", sentenceCorrect:"Грамматика и пунктуация корректны.", sentenceError:"В предложении есть ошибки.", sentenceNeedWord:"Используйте слово с карточки в предложении.", sentenceServiceError:"Сервис проверки временно недоступен. Попробуйте ещё раз.", correctedLabel:"Возможный исправленный вариант", sentenceTranslation:"Перевод предложения", empty:"Пока нет карточек. Добавьте первое английское слово.", noMatch:"По этому запросу карточек не найдено.", added:"Карточка добавлена.", deleted:"Карточка удалена.", autoError:"Не удалось получить перевод автоматически. Укажите перевод вручную.", translating:"Ищем перевод…", duplicate:"Это слово уже есть в ваших карточках.", footerTagline:"Практика маленькими шагами.", deleteAria:"Удалить карточку", learnedAria:"Отметить как выученную", learningAria:"Вернуть в изучение", count:"карточек", sessionDone:"Сессия завершена. Отличная работа!"
  },
  en: {
    navFinder:"College Finder", navPractice:"Practice", navRoi:"ROI calculator", back:"Back to directory", eyebrow:"FULLRIDE / PRACTICE", title:"Learn words<br /><em>at your pace.</em>", intro:"Add unfamiliar English words, save them, and return to them in short random practice sessions.", saved:"cards saved", learned:"learned", addLabel:"NEW CARD", addTitle:"Add a word to practice", wordLabel:"English word", translationLabel:"Translation", optional:"(editable)", addButton:"Add card", translationNote:"A translation is requested automatically. You can edit it before saving.", libraryLabel:"MY CARDS", libraryTitle:"Review what matters", startButton:"Start practice", searchPlaceholder:"Search words and translations", filterAll:"All cards", filterLearning:"In progress", filterLearned:"Learned", sessionLabel:"SESSION", sessionTitle:"Recall the translation", revealButton:"Show translation", repeatButton:"Repeat", knowButton:"I know it", nextButton:"Next", sentenceLabel:"Your sentence", sentencePlaceholder:"Write an English sentence using this word", sentenceHint:"Write a complete English sentence to continue.", checkSentence:"Check sentence", checkingSentence:"Checking grammar…", sentenceCorrect:"Grammar and punctuation look correct.", sentenceError:"There are issues in this sentence.", sentenceNeedWord:"Use the card word in your sentence.", sentenceServiceError:"The grammar service is temporarily unavailable. Try again.", correctedLabel:"Suggested correction", sentenceTranslation:"Sentence translation", empty:"No cards yet. Add your first English word.", noMatch:"No cards match this search.", added:"Card added.", deleted:"Card deleted.", autoError:"Automatic translation failed. Enter a translation manually.", translating:"Finding a translation…", duplicate:"This word is already in your cards.", footerTagline:"Small steps, steady practice.", deleteAria:"Delete card", learnedAria:"Mark as learned", learningAria:"Return to learning", count:"cards", sessionDone:"Session complete. Great work!"
  }
};

Object.assign(practiceCopy.uk, { decksLabel:"ГОТОВІ НАБОРИ", decksTitle:"Оберіть свій рівень", decksIntro:"У кожному наборі 500 слів. За одне тренування ви отримаєте 20 випадкових карток обраного рівня.", packWords:"слів", packLearned:"вивчено", packStart:"Почати", packReview:"Повторити", packDone:"Ви вивчили всі слова цього набору. Можна повторити їх ще раз.", packSessionDone:"Тренування завершено. Поверніться до набору, щоб отримати нові слова.", packTranslateError:"Не вдалося отримати переклад. Спробуйте ще раз.", packTranslating:"Перекладаємо…", packA1:"Базові слова для щоденних ситуацій.", packA2:"Більше тем для розмови й простих текстів.", packB1:"Самостійне спілкування та знайомі теми.", packB2:"Складніші тексти, навчання й аргументація.", packC1:"Точна мова для навчання і професійних тем.", packNative:"Складна лексика рівня C2 для впевненого володіння мовою.", packA1A2:"250 слів A1 і 250 слів A2 для плавного переходу.", packA2B1:"250 слів A2 і 250 слів B1 для наступного кроку.", packB1B2:"250 слів B1 і 250 слів B2 для переходу до складніших тем." });
Object.assign(practiceCopy.ru, { decksLabel:"ГОТОВЫЕ НАБОРЫ", decksTitle:"Выберите свой уровень", decksIntro:"В каждом наборе 500 слов. За одну тренировку вы получите 20 случайных карточек выбранного уровня.", packWords:"слов", packLearned:"выучено", packStart:"Начать", packReview:"Повторить", packDone:"Вы выучили все слова этого набора. Можно повторить их ещё раз.", packSessionDone:"Тренировка завершена. Вернитесь к набору за новыми словами.", packTranslateError:"Не удалось получить перевод. Попробуйте ещё раз.", packTranslating:"Переводим…", packA1:"Базовые слова для повседневных ситуаций.", packA2:"Больше тем для разговора и простых текстов.", packB1:"Самостоятельное общение и знакомые темы.", packB2:"Более сложные тексты, учёба и аргументация.", packC1:"Точная речь для учёбы и профессиональных тем.", packNative:"Сложная лексика уровня C2 для уверенного владения языком.", packA1A2:"250 слов A1 и 250 слов A2 для плавного перехода.", packA2B1:"250 слов A2 и 250 слов B1 для следующего шага.", packB1B2:"250 слов B1 и 250 слов B2 для перехода к сложным темам." });
Object.assign(practiceCopy.en, { decksLabel:"READY-MADE DECKS", decksTitle:"Choose your level", decksIntro:"Each deck has 500 words. Each session draws 20 random cards from the selected level.", packWords:"words", packLearned:"learned", packStart:"Start", packReview:"Review", packDone:"You have learned every word in this deck. You can review them again.", packSessionDone:"Session complete. Return to the deck for more words.", packTranslateError:"Translation is unavailable. Please try again.", packTranslating:"Translating…", packA1:"Everyday words for familiar situations.", packA2:"More topics for conversations and simple texts.", packB1:"Independent communication on familiar topics.", packB2:"More complex texts, study and discussion.", packC1:"Precise language for academic and professional contexts.", packNative:"Challenging C2 vocabulary for proficient speakers.", packA1A2:"250 A1 words and 250 A2 words for a smooth transition.", packA2B1:"250 A2 words and 250 B1 words for the next step.", packB1B2:"250 B1 words and 250 B2 words for more complex topics." });

Object.assign(practiceCopy.uk, {
  bulkLabel:"Або вставте список слів / виразів", bulkButton:"Додати список", bulkNote:"Дублікати пропускаються. Картки зберігаються одразу; переклад і словникові дані завантажуються у фоні.", sourceLabel:"Джерело (необов'язково)", bulkAdded:"Додано {count} карток. Дані завантажуються у фоні.", bulkEmpty:"Вставте англійські слова, розділені комами або рядками.", bulkLimit:"У бібліотеці може бути до 500 особистих карток.",
  statNew:"Нові", statLearning:"У процесі", statDue:"На повтор сьогодні", statMastered:"Закріплено", statReviews:"Повторень сьогодні", statStreak:"Днів поспіль", filterNew:"Нові", filterDue:"На повтор сьогодні", filterMastered:"Закріплено", reviewFive:"5-денний огляд", reviewMonth:"Огляд місяця", reviewEmpty:"Для цього огляду ще немає слів.",
  revealButton:"Показати значення", sentenceHint:"Речення допоможе закріпити слово, але його можна пропустити.", rateHint:"Як добре ви згадали значення? Речення можна пропустити.", rateAgain:"Again", rateHard:"Hard", rateGood:"Good", rateEasy:"Easy", translationPending:"Переклад завантажується…", translationUnavailable:"Переклад недоступний. Можна продовжити або додати його пізніше.", definitionLabel:"Значення англійською", examplesLabel:"Приклади", synonymsLabel:"Синоніми / перефразування", noExamples:"Для цього слова поки немає перевірених прикладів.", nextDue:"Наступний повтор: {date}", sentenceWarning:"Речення зрозуміле, але є зауваження. Можна виправити або продовжити.", sentenceUnchecked:"Сенс і природність формулювання автоматично не підтверджені.", sentenceServiceError:"Перевірка недоступна; ви все одно можете продовжити.", sentenceNeedWord:"Використайте слово або його граматичну форму в реченні."
});
Object.assign(practiceCopy.ru, {
  bulkLabel:"Или вставьте список слов / выражений", bulkButton:"Добавить список", bulkNote:"Дубликаты пропускаются. Карточки сохраняются сразу; перевод и словарные данные загружаются в фоне.", sourceLabel:"Источник (необязательно)", bulkAdded:"Добавлено {count} карточек. Данные загружаются в фоне.", bulkEmpty:"Вставьте английские слова через запятую или с новой строки.", bulkLimit:"В библиотеке может быть до 500 личных карточек.",
  statNew:"Новые", statLearning:"В процессе", statDue:"К повтору сегодня", statMastered:"Закреплено", statReviews:"Повторов сегодня", statStreak:"Дней подряд", filterNew:"Новые", filterDue:"К повтору сегодня", filterMastered:"Закреплено", reviewFive:"5-дневный обзор", reviewMonth:"Обзор месяца", reviewEmpty:"Для этого обзора пока нет слов.",
  revealButton:"Показать значение", sentenceHint:"Предложение поможет закрепить слово, но его можно пропустить.", rateHint:"Насколько хорошо вспомнили значение? Предложение можно пропустить.", rateAgain:"Again", rateHard:"Hard", rateGood:"Good", rateEasy:"Easy", translationPending:"Перевод загружается…", translationUnavailable:"Перевод недоступен. Можно продолжить или добавить его позже.", definitionLabel:"Значение на английском", examplesLabel:"Примеры", synonymsLabel:"Синонимы / перефразирование", noExamples:"Пока нет проверенных примеров для этого слова.", nextDue:"Следующий повтор: {date}", sentenceWarning:"Предложение понятно, но есть замечания. Можно исправить или продолжить.", sentenceUnchecked:"Смысл и естественность формулировки автоматически не подтверждены.", sentenceServiceError:"Проверка недоступна; вы всё равно можете продолжить.", sentenceNeedWord:"Используйте слово или его грамматическую форму в предложении."
});
Object.assign(practiceCopy.en, {
  bulkLabel:"Or paste a list of words / phrases", bulkButton:"Add list", bulkNote:"Duplicates are skipped. Cards save immediately; translations and dictionary details load in the background.", sourceLabel:"Source (optional)", bulkAdded:"Added {count} cards. Details are loading in the background.", bulkEmpty:"Paste English words separated by commas or new lines.", bulkLimit:"You can save up to 500 personal cards.",
  statNew:"New", statLearning:"Learning", statDue:"Due today", statMastered:"Mastered", statReviews:"Reviews today", statStreak:"Day streak", filterNew:"New", filterDue:"Due today", filterMastered:"Mastered", reviewFive:"5-Day Review", reviewMonth:"Monthly Review", reviewEmpty:"No words are available for this review yet.",
  revealButton:"Show meaning", sentenceHint:"A sentence helps you remember, but you can skip it.", rateHint:"How well did you recall the meaning? The sentence is optional.", rateAgain:"Again", rateHard:"Hard", rateGood:"Good", rateEasy:"Easy", translationPending:"Loading translation…", translationUnavailable:"Translation unavailable. You can continue or add it later.", definitionLabel:"English definition", examplesLabel:"Examples", synonymsLabel:"Synonyms / paraphrases", noExamples:"No sourced examples for this word yet.", nextDue:"Next review: {date}", sentenceWarning:"Understandable, but there are issues. You can revise or continue.", sentenceUnchecked:"Meaning and naturalness are not automatically verified.", sentenceServiceError:"Checking is unavailable; you can still continue.", sentenceNeedWord:"Use the word or an inflected form in your sentence."
});

const core = globalThis.FullRidePracticeCore;
Object.assign(practiceCopy.uk, { grammarLabel:"Граматика", meaningLabel:"Вживання слова", naturalLabel:"Природність", formFound:"Форму слова знайдено; точність значення не перевірена", notVerified:"Без AI-перевірки не підтверджено", grammarOkay:"Зауважень не знайдено", grammarIssues:"Є зауваження — перегляньте варіант нижче" });
Object.assign(practiceCopy.ru, { grammarLabel:"Грамматика", meaningLabel:"Употребление слова", naturalLabel:"Естественность", formFound:"Форма слова найдена; точность смысла не проверена", notVerified:"Без AI-проверки не подтверждено", grammarOkay:"Замечаний не найдено", grammarIssues:"Есть замечания — посмотрите вариант ниже" });
Object.assign(practiceCopy.en, { grammarLabel:"Grammar", meaningLabel:"Word use", naturalLabel:"Naturalness", formFound:"Word form found; meaning not verified", notVerified:"Not verified without AI", grammarOkay:"No issues found", grammarIssues:"Review the suggested changes below" });
const deckDefinitions = [
  { id:"A1", title:"A1", copy:"packA1" }, { id:"A2", title:"A2", copy:"packA2" },
  { id:"B1", title:"B1", copy:"packB1" }, { id:"B2", title:"B2", copy:"packB2" },
  { id:"C1", title:"C1", copy:"packC1" }, { id:"C2", title:"Native", copy:"packNative" },
  { id:"A1-A2", title:"A1 → A2", copy:"packA1A2", bridge:true },
  { id:"A2-B1", title:"A2 → B1", copy:"packA2B1", bridge:true },
  { id:"B1-B2", title:"B1 → B2", copy:"packB1B2", bridge:true }
];
const baseDecks = globalThis.FullRidePracticePacks || {};
const deckProgressKey = "fullride-pack-learned-v1";
const deckTranslationKey = "fullride-pack-translations-v1";
const deckSrsKey = "fullride-pack-srs-v1";
function loadDeckProgress() { try { const saved = JSON.parse(localStorage.getItem(deckProgressKey) || "[]"); return new Set(Array.isArray(saved) ? saved.filter(id => typeof id === "string") : []); } catch { return new Set(); } }
function loadDeckTranslations() { try { const saved = JSON.parse(localStorage.getItem(deckTranslationKey) || "{}"); return saved && typeof saved === "object" && !Array.isArray(saved) ? saved : {}; } catch { return {}; } }
function loadDeckSrs() { try { const saved = JSON.parse(localStorage.getItem(deckSrsKey) || "{}"); return saved && typeof saved === "object" && !Array.isArray(saved) ? saved : {}; } catch { return {}; } }
let learnedDeckWords = loadDeckProgress();
let deckTranslations = loadDeckTranslations();
let deckSrs = loadDeckSrs();
let activeDeckId = null;
function deckEntries(id) {
  const bridge = id.split("-");
  const levels = bridge.length === 2 ? bridge : [id];
  return levels.flatMap(level => {
    const words = baseDecks[level] || [];
    const chosen = bridge.length === 2 ? (level === bridge[0] ? words.slice(250) : words.slice(0, 250)) : words;
    return chosen.map(word => ({ id:`${level}:${word}`, word }));
  });
}
function saveDeckProgress() { localStorage.setItem(deckProgressKey, JSON.stringify([...learnedDeckWords])); window.dispatchEvent(new CustomEvent("fullride:local-data-changed", { detail:{ kind:"packLearned" } })); }
function srsForDeck(id) { return core.hydrateSrs(deckSrs[id], learnedDeckWords.has(id), Date.now()); }
function saveDeckSrs() { localStorage.setItem(deckSrsKey, JSON.stringify(deckSrs)); }
function renderDecks() {
  pq("practice-deck-list").innerHTML = deckDefinitions.map(deck => {
    const words = deckEntries(deck.id);
    const learned = words.filter(word => srsForDeck(word.id).status !== "new").length;
    return `<article class="practice-deck ${deck.bridge ? `practice-deck--bridge practice-deck--bridge-${deck.id.toLowerCase()}` : `practice-deck--${deck.id.toLowerCase()}`}"><div class="practice-deck__top"><span class="practice-deck__level">${deck.bridge ? "BRIDGE" : "CEFR"}</span><span class="practice-deck__count">${words.length} ${pc("packWords")}</span></div><h3>${deck.title}</h3><p>${pc(deck.copy)}</p><div class="practice-deck__progress" role="progressbar" aria-valuenow="${learned}" aria-valuemin="0" aria-valuemax="500" aria-label="${deck.title}"><span style="width:${learned / 5}%"></span></div><div class="practice-deck__bottom"><small>${learned} / 500 ${pc("packLearned")}</small><button type="button" data-start-deck="${deck.id}" ${words.length !== 500 ? "disabled" : ""}>${pc(learned === 500 ? "packReview" : "packStart")} →</button></div></article>`;
  }).join("");
}

Object.assign(practiceCopy.uk, { translationNote:"Переклад завантажується у фоні. Якщо сервіс недоступний, додайте його вручну через кнопку редагування картки.", editTranslation:"Редагувати переклад", saveTranslation:"Зберегти переклад" });
Object.assign(practiceCopy.ru, { translationNote:"Перевод загружается в фоне. Если сервис недоступен, добавьте его вручную через кнопку редактирования карточки.", editTranslation:"Изменить перевод", saveTranslation:"Сохранить перевод" });
Object.assign(practiceCopy.en, { translationNote:"Translation loads in the background. If the service is unavailable, edit the saved card manually.", editTranslation:"Edit translation", saveTranslation:"Save translation" });

const flashcardKey = "fullride-flashcards-v1";
let practiceLanguage = ["uk", "ru", "en"].includes(new URLSearchParams(location.search).get("lang")) ? new URLSearchParams(location.search).get("lang") : "uk";
let flashcards = loadCards();
let searchTerm = "";
let filterMode = "all";
let practiceQueue = [];
let practiceIndex = 0;
let sessionMode = "due";
let meaningRevealed = false;
const pq = id => document.getElementById(id);
const pc = key => practiceCopy[practiceLanguage][key] || practiceCopy.en[key] || key;

function normalize(value) { return String(value || "").trim().toLocaleLowerCase(); }
function escapeHtml(value) { return String(value).replace(/[&<>"']/g, character => ({"&":"&amp;", "<":"&lt;", ">":"&gt;", "\"":"&quot;", "'":"&#39;"}[character])); }
function loadCards() {
  try {
    const value = JSON.parse(localStorage.getItem(flashcardKey) || "[]");
    if (!Array.isArray(value)) return [];
    return value.filter(card => card && typeof card.id === "string" && typeof card.word === "string")
      .map(card => {
        const createdAt = typeof card.createdAt === "number" ? card.createdAt : Date.now();
        const translation = String(card.translation || "").trim().slice(0,240);
        const translations = card.translations && typeof card.translations === "object" && !Array.isArray(card.translations) ? card.translations : {};
        if (translation && !Object.keys(translations).length) translations[practiceLanguage] = translation;
        return {
          id:card.id.slice(0,80), word:card.word.trim().slice(0,120), translation, translations, language:"en",
          definition:String(card.definition || "").slice(0,300),
          examples:Array.isArray(card.examples) ? card.examples.filter(item => typeof item === "string").slice(0,3) : [],
          synonyms:Array.isArray(card.synonyms) ? card.synonyms.filter(item => typeof item === "string").slice(0,8) : [],
          source:String(card.source || "").slice(0,80), cefr:String(card.cefr || "").slice(0,8),
          metadataStatus:String(card.metadataStatus || "pending").slice(0,24),
          learned:Boolean(card.learned), createdAt, srs:core.hydrateSrs(card.srs, card.learned, createdAt),
          sentence:typeof card.sentence === "string" ? card.sentence.slice(0,500) : "",
          sentenceStatus:["idle","correct","needs-review"].includes(card.sentenceStatus) ? card.sentenceStatus : "idle",
          sentenceCorrected:typeof card.sentenceCorrected === "string" ? card.sentenceCorrected.slice(0,500) : "",
          sentenceTranslation:typeof card.sentenceTranslation === "string" ? card.sentenceTranslation.slice(0,700) : "",
          sentenceTranslationLanguage:["uk","ru","en"].includes(card.sentenceTranslationLanguage) ? card.sentenceTranslationLanguage : ""
        };
      }).filter(card => card.word).slice(0,500);
  } catch { return []; }
}
function saveCards() { localStorage.setItem(flashcardKey, JSON.stringify(flashcards)); window.dispatchEvent(new CustomEvent("fullride:local-data-changed", { detail:{ kind:"flashcards" } })); }
let enrichSyncTimer = null;
function saveEnrichedCards() {
  localStorage.setItem(flashcardKey, JSON.stringify(flashcards));
  window.clearTimeout(enrichSyncTimer);
  enrichSyncTimer = window.setTimeout(() => window.dispatchEvent(new CustomEvent("fullride:local-data-changed", { detail:{ kind:"flashcards" } })), 3000);
}
function cardTranslation(card) { return practiceLanguage === "en" ? "" : String(card.translations?.[practiceLanguage] || "").slice(0,240); }
function visibleCards() { const now = Date.now(); return flashcards.filter(card => {
  const matchesSearch = !searchTerm || normalize([card.word,card.translation,card.definition,card.sentence,...card.synonyms].join(" ")).includes(normalize(searchTerm));
  const matchesFilter = filterMode === "all" || (filterMode === "due" ? card.srs.status !== "new" && card.srs.dueAt <= now : card.srs.status === filterMode);
  return matchesSearch && matchesFilter;
}); }
function decodeHtml(value) { const area = document.createElement("textarea"); area.innerHTML = value; return area.value; }
function setStatus(message, error = false) { pq("card-status").textContent = message; pq("card-status").classList.toggle("is-error", error); }
const translationRequests = new Map();
async function translateText(text, target = practiceLanguage) {
  if (target === "en") return text;
  const cacheKey = target + ":" + normalize(text);
  const oldDeckKey = target + ":" + (cefrByWord.get(normalize(text)) || "") + ":" + normalize(text);
  if (text.length <= 120 && (deckTranslations[cacheKey] || deckTranslations[oldDeckKey])) return deckTranslations[cacheKey] || deckTranslations[oldDeckKey];
  if (translationRequests.has(cacheKey)) return translationRequests.get(cacheKey);
  const request = (async () => {
    const controller = new AbortController();
    const timeout = window.setTimeout(() => controller.abort(), 4500);
    try {
      const response = await fetch("https://api.mymemory.translated.net/get?q=" + encodeURIComponent(text) + "&langpair=en|" + target, { signal:controller.signal });
      if (!response.ok) return "";
      const data = await response.json();
      const result = decodeHtml(data?.responseData?.translatedText || "").trim();
      if (!result || data?.responseStatus !== 200 || normalize(result) === normalize(text)) return "";
      if (text.length <= 120) {
        deckTranslations[cacheKey] = result.slice(0,240);
        try { localStorage.setItem(deckTranslationKey, JSON.stringify(deckTranslations)); } catch {}
      }
      return result;
    } catch { return ""; }
    finally { window.clearTimeout(timeout); translationRequests.delete(cacheKey); }
  })();
  translationRequests.set(cacheKey, request);
  return request;
}
async function checkGrammar(text) { const controller = new AbortController(); const timeout = window.setTimeout(() => controller.abort(), 4500); try { const response = await fetch("https://api.languagetool.org/v2/check", { method:"POST", headers:{"Content-Type":"application/x-www-form-urlencoded;charset=UTF-8"}, body:new URLSearchParams({ text, language:"en-US", enabledOnly:"false" }), signal:controller.signal }); if (!response.ok) throw new Error("grammar-service"); return response.json(); } finally { window.clearTimeout(timeout); } }
function correctedText(text, matches = []) { return [...matches].sort((a,b) => b.offset - a.offset).reduce((result, match) => { const replacement = match.replacements?.[0]?.value; return replacement === undefined ? result : `${result.slice(0,match.offset)}${replacement}${result.slice(match.offset + match.length)}`; }, text); }
const cefrByWord = new Map(Object.entries(baseDecks).flatMap(([level, words]) => words.map(word => [normalize(word),level])));
const dictionaryRequests = new Map();
async function dictionaryDetails(word) {
  if (!/^[a-z'-]+$/i.test(word)) return null;
  const key = normalize(word);
  if (dictionaryRequests.has(key)) return dictionaryRequests.get(key);
  const request = (async () => {
    const controller = new AbortController();
    const timeout = window.setTimeout(() => controller.abort(), 4500);
    try {
      const response = await fetch("https://api.dictionaryapi.dev/api/v2/entries/en/" + encodeURIComponent(word), { signal:controller.signal });
      if (!response.ok) return null;
      const data = await response.json();
      const meanings = Array.isArray(data) ? data.flatMap(entry => entry.meanings || []) : [];
      const definitions = meanings.flatMap(meaning => meaning.definitions || []);
      return {
        definition:String(definitions[0]?.definition || "").slice(0,300),
        examples:[...new Set(definitions.map(item => item.example).filter(item => typeof item === "string"))].slice(0,3).map(item => item.slice(0,240)),
        synonyms:[...new Set([...meanings.flatMap(item => item.synonyms || []), ...definitions.flatMap(item => item.synonyms || [])].filter(item => typeof item === "string"))].slice(0,8).map(item => item.slice(0,60))
      };
    } catch { return null; }
    finally { window.clearTimeout(timeout); dictionaryRequests.delete(key); }
  })();
  dictionaryRequests.set(key, request);
  return request;
}
const enrichmentQueue = [];
const queuedEnrichment = new Set();
let activeEnrichment = 0;
function queueEnrichment(cards, priority = false) {
  for (const card of cards) {
    if (queuedEnrichment.has(card.id)) {
      if (priority) {
        const index = enrichmentQueue.indexOf(card);
        if (index >= 0) enrichmentQueue.splice(index, 1);
        else continue;
      } else continue;
    }
    if (["ready","unavailable"].includes(card.metadataStatus) && (practiceLanguage === "en" || cardTranslation(card))) continue;
    queuedEnrichment.add(card.id);
    if (priority) enrichmentQueue.unshift(card);
    else enrichmentQueue.push(card);
  }
  while (activeEnrichment < 2 && enrichmentQueue.length) {
    const card = enrichmentQueue.shift();
    activeEnrichment += 1;
    enrichCard(card).finally(() => { activeEnrichment -= 1; queuedEnrichment.delete(card.id); queueEnrichment([]); });
  }
}
async function enrichCard(card, target = practiceLanguage) {
  const needsDictionary = card.metadataStatus !== "ready" && card.metadataStatus !== "unavailable";
  const [translation, details] = await Promise.all([
    target === "en" || card.translations?.[target] ? Promise.resolve("") : translateText(card.word, target),
    needsDictionary ? dictionaryDetails(card.word) : Promise.resolve(null)
  ]);
  if (translation && !card.translations?.[target]) {
    card.translations = { ...card.translations, [target]:translation.slice(0,240) };
    card.translation = translation.slice(0,240);
  }
  if (needsDictionary) {
    card.definition = details?.definition || "";
    card.examples = details?.examples || [];
    card.synonyms = details?.synonyms || [];
    card.metadataStatus = details ? "ready" : "unavailable";
  }
  if (flashcards.includes(card)) saveEnrichedCards();
  if (currentSessionCard() === card && meaningRevealed) renderMeaning(card);
  if (!document.activeElement.closest(".flashcard-row__edit")) render();
}
function renderMeaning(card) {
  const translation = cardTranslation(card);
  pq("session-translation").textContent = practiceLanguage === "en" ? "" : translation || pc(card.metadataStatus === "pending" ? "translationPending" : "translationUnavailable");
  pq("session-translation").hidden = practiceLanguage === "en";
  pq("session-definition").textContent = card.definition ? pc("definitionLabel") + ": " + card.definition : "";
  pq("session-definition").hidden = !card.definition;
  pq("session-examples").innerHTML = card.examples.map(example => "<li>" + escapeHtml(example) + "</li>").join("");
  pq("session-examples").hidden = !card.examples.length;
  pq("session-synonyms").textContent = card.synonyms.length ? pc("synonymsLabel") + ": " + card.synonyms.join(" · ") : "";
  pq("session-synonyms").hidden = !card.synonyms.length;
  pq("session-source").textContent = [card.cefr,card.source,card.metadataStatus === "ready" ? "Dictionary API" : ""].filter(Boolean).join(" · ");
  pq("session-source").hidden = !card.cefr && !card.source && card.metadataStatus !== "ready";
}
function renderLanguage() { document.documentElement.lang = practiceLanguage; document.querySelectorAll("[data-practice-key]").forEach(element => { element.innerHTML = pc(element.dataset.practiceKey); }); document.querySelectorAll("[data-practice-placeholder]").forEach(element => { element.placeholder = pc(element.dataset.practicePlaceholder); }); document.querySelectorAll("[data-practice-lang]").forEach(button => button.classList.toggle("active", button.dataset.practiceLang === practiceLanguage)); pq("word-input").placeholder = practiceLanguage === "en" ? "for example, scholarship" : "например, scholarship"; renderDecks(); render(); queueEnrichment(flashcards.slice(0,120)); }
function sentenceBadge(card) { if (card.sentenceStatus === "correct") return `<span class="sentence-badge is-correct">✓ ${escapeHtml(pc("sentenceCorrect"))}</span>`; if (card.sentenceStatus === "needs-review") return `<span class="sentence-badge is-review">! ${escapeHtml(pc("sentenceError"))}</span>`; return ""; }
function render() {
  const now = Date.now();
  const reviewed = flashcards.filter(card => card.srs.status !== "new").length;
  const counts = {
    new:flashcards.filter(card => card.srs.status === "new").length,
    learning:flashcards.filter(card => card.srs.status === "learning" || card.srs.status === "review").length,
    due:flashcards.filter(card => card.srs.status !== "new" && card.srs.dueAt <= now).length,
    mastered:flashcards.filter(card => card.srs.status === "mastered").length
  };
  const today = new Date().setHours(0,0,0,0);
  const reviewDays = new Set(flashcards.flatMap(card => card.srs.history.map(entry => new Date(entry.at).setHours(0,0,0,0))));
  let streak = 0;
  let day = reviewDays.has(today) ? today : today - core.DAY;
  while (reviewDays.has(day)) { streak += 1; day -= core.DAY; }
  pq("practice-total").textContent = flashcards.length;
  pq("practice-learned").textContent = reviewed;
  for (const [key,value] of Object.entries(counts)) pq("stat-" + key).textContent = value;
  pq("stat-reviews").textContent = flashcards.reduce((sum,card) => sum + card.srs.history.filter(entry => entry.at >= today).length, 0);
  pq("stat-streak").textContent = streak;
  const cards = visibleCards();
  pq("card-result-count").textContent = cards.length + " " + pc("count");
  pq("card-list").innerHTML = cards.length ? cards.map(card => {
    const due = card.srs.status === "new" ? pc("statNew") : new Date(card.srs.dueAt).toLocaleDateString(practiceLanguage);
    const translation = cardTranslation(card) || (practiceLanguage === "en" ? card.definition : pc(card.metadataStatus === "pending" ? "translationPending" : "translationUnavailable"));
    return '<article class="flashcard-row ' + (card.srs.status === "mastered" ? "is-learned" : "") + '">' +
      '<div class="flashcard-row__word"><small>' + escapeHtml(card.cefr || "EN") + '</small><strong>' + escapeHtml(card.word) + '</strong><small>' + escapeHtml(card.source) + '</small></div>' +
      '<div class="flashcard-row__translation"><small>' + escapeHtml(pc("nextDue").replace("{date}",due)) + '</small><div>' + escapeHtml(translation) + '</div></div>' +
      '<div class="flashcard-row__actions"><button type="button" data-card-learn="' + escapeHtml(card.id) + '" aria-label="' + escapeHtml(card.learned ? pc("learningAria") : pc("learnedAria")) + '">' + (card.learned ? "↶" : "✓") + '</button>' +
      (practiceLanguage === "en" ? "" : '<button type="button" data-card-edit="' + escapeHtml(card.id) + '" aria-label="' + escapeHtml(pc("editTranslation")) + '">✎</button>') +
      '<button type="button" data-card-delete="' + escapeHtml(card.id) + '" aria-label="' + escapeHtml(pc("deleteAria")) + '">×</button></div>' +
      (practiceLanguage === "en" ? "" : '<form class="flashcard-row__edit" data-card-edit-form="' + escapeHtml(card.id) + '" hidden><input aria-label="' + escapeHtml(pc("translationLabel")) + '" maxlength="240" value="' + escapeHtml(cardTranslation(card)) + '" required /><button type="submit">' + escapeHtml(pc("saveTranslation")) + '</button></form>') +
      (card.sentence ? '<div class="flashcard-row__sentence"><small>' + escapeHtml(pc("sentenceLabel")) + '</small><p>' + escapeHtml(card.sentence) + '</p>' + sentenceBadge(card) + '</div>' : "") + '</article>';
  }).join("") : '<p class="card-empty">' + escapeHtml(pc(searchTerm || filterMode !== "all" ? "noMatch" : "empty")) + '</p>';
}
function closePractice() { pq("practice-modal").classList.remove("is-open"); pq("practice-modal").setAttribute("aria-hidden","true"); activeDeckId = null; practiceQueue = []; practiceIndex = 0; meaningRevealed = false; }
function currentSessionCard() { return practiceQueue[practiceIndex]; }
function renderSentenceState(card) {
  pq("session-sentence").value = card.sentence || "";
  pq("sentence-feedback").textContent = card.sentenceStatus === "correct" ? pc("sentenceCorrect") + " " + pc("sentenceUnchecked") : card.sentenceStatus === "needs-review" ? pc("sentenceWarning") : "";
  pq("sentence-feedback").className = "sentence-feedback" + (card.sentenceStatus === "correct" ? " is-correct" : card.sentenceStatus === "needs-review" ? " is-error" : "");
  pq("sentence-corrected").hidden = !card.sentenceCorrected || card.sentenceStatus !== "needs-review";
  if (!pq("sentence-corrected").hidden) pq("sentence-corrected").innerHTML = "<small>" + escapeHtml(pc("correctedLabel")) + "</small><strong>" + escapeHtml(card.sentenceCorrected) + "</strong>";
  pq("sentence-translation").hidden = practiceLanguage === "en" || card.sentenceTranslationLanguage !== practiceLanguage || !card.sentenceTranslation;
  pq("sentence-translation").textContent = card.sentenceTranslation || "";
}
async function refreshSessionTranslation() {
  const card = currentSessionCard();
  if (!card || !card.sentence || practiceLanguage === "en") return;
  const translated = await translateText(card.sentence);
  if (!translated || card !== currentSessionCard()) return;
  card.sentenceTranslation = translated;
  card.sentenceTranslationLanguage = practiceLanguage;
  if (!activeDeckId) saveCards();
  pq("sentence-translation").textContent = translated;
  pq("sentence-translation").hidden = false;
}
function updateSession() {
  const card = currentSessionCard();
  if (!card) { const wasDeck = Boolean(activeDeckId); closePractice(); if (wasDeck) pq("practice-deck-status").textContent = pc("packSessionDone"); else setStatus(pc("sessionDone")); return; }
  pq("session-title").textContent = activeDeckId ? deckDefinitions.find(deck => deck.id === activeDeckId)?.title || activeDeckId : pc("sessionTitle");
  pq("session-word").textContent = card.word;
  meaningRevealed = false;
  pq("session-meaning").hidden = true;
  pq("session-actions").hidden = true;
  pq("session-reveal").hidden = false;
  pq("session-reveal").disabled = false;
  pq("session-reveal").textContent = pc("revealButton");
  pq("session-counter").textContent = (practiceIndex + 1) + " / " + practiceQueue.length;
  pq("session-progress").style.setProperty("--session-progress", (((practiceIndex + 1) / practiceQueue.length) * 100) + "%");
  renderSentenceState(card);
  queueEnrichment([card], true);
}
function shuffled(items) { const result = [...items]; for (let index = result.length - 1; index > 0; index -= 1) { const other = Math.floor(Math.random() * (index + 1)); [result[index], result[other]] = [result[other], result[index]]; } return result; }
function openSession() { practiceIndex = 0; pq("practice-modal").classList.add("is-open"); pq("practice-modal").setAttribute("aria-hidden","false"); updateSession(); }
function startPractice() { activeDeckId = null; sessionMode = "due"; practiceQueue = core.selectSession(visibleCards()); if (!practiceQueue.length) { setStatus(pc("reviewEmpty"), true); return; } openSession(); }
function startReview(mode) {
  const now = Date.now();
  const start = mode === "five" ? now - 5 * core.DAY : new Date(new Date().getFullYear(), new Date().getMonth(), 1).getTime();
  const cards = flashcards.filter(card => card.srs.firstReviewedAt && card.srs.firstReviewedAt >= start);
  if (!cards.length) { setStatus(pc("reviewEmpty"), true); return; }
  activeDeckId = null;
  sessionMode = mode;
  practiceQueue = shuffled(cards).slice(0,20);
  openSession();
}
function startDeck(id) {
  const words = deckEntries(id);
  if (words.length !== 500) return;
  const entries = words.map(entry => ({ ...entry, createdAt:Date.now(), srs:srsForDeck(entry.id) }));
  const chosen = core.selectSession(shuffled(entries));
  if (!chosen.length) { pq("practice-deck-status").textContent = pc("packDone"); return; }
  activeDeckId = id;
  sessionMode = "due";
  practiceQueue = chosen.map(entry => ({ ...entry, translation:"", translations:{}, definition:"", examples:[], synonyms:[], metadataStatus:"pending", cefr:entry.id.split(":")[0], source:"", sentence:"", sentenceStatus:"idle", sentenceCorrected:"", sentenceTranslation:"", sentenceTranslationLanguage:"" }));
  pq("practice-deck-status").textContent = "";
  openSession();
}
function nextSession(rating) {
  const card = currentSessionCard();
  if (!card || !meaningRevealed || !["again","hard","good","easy"].includes(rating)) return;
  card.srs = core.scheduleReview(card.srs, rating);
  card.learned = rating === "good" || rating === "easy";
  if (activeDeckId) {
    deckSrs[card.id] = card.srs;
    saveDeckSrs();
    if (card.learned) learnedDeckWords.add(card.id);
    else learnedDeckWords.delete(card.id);
    saveDeckProgress();
    renderDecks();
  } else { saveCards(); render(); }
  practiceIndex += 1;
  updateSession();
}

function makeCard(word, translation = "", source = "") {
  const createdAt = Date.now();
  return {
    id:"card-" + createdAt + "-" + Math.random().toString(36).slice(2,10), word, translation,
    translations:translation && practiceLanguage !== "en" ? {[practiceLanguage]:translation} : {},
    language:"en", definition:"", examples:[], synonyms:[], source:source.slice(0,80),
    cefr:cefrByWord.get(normalize(word)) || "", metadataStatus:"pending",
    learned:false, createdAt, srs:core.initialSrs(false, createdAt),
    sentence:"", sentenceStatus:"idle", sentenceCorrected:"", sentenceTranslation:"", sentenceTranslationLanguage:""
  };
}
pq("card-form").addEventListener("submit", event => {
  event.preventDefault();
  const word = pq("word-input").value.trim().replace(/\s+/g," ");
  const translation = pq("translation-input").value.trim();
  if (!word) return;
  if (flashcards.some(card => normalize(card.word) === normalize(word))) return setStatus(pc("duplicate"), true);
  if (flashcards.length >= 500) return setStatus(pc("bulkLimit"), true);
  const card = makeCard(word, translation);
  flashcards.unshift(card);
  saveCards(); event.currentTarget.reset(); setStatus(pc("added")); render();
  queueEnrichment([card], true);
});
pq("bulk-form").addEventListener("submit", event => {
  event.preventDefault();
  const parsed = core.parseWords(pq("bulk-input").value);
  if (!parsed.length) return setStatus(pc("bulkEmpty"), true);
  const existing = new Set(flashcards.map(card => normalize(card.word)));
  const source = pq("bulk-source").value.trim();
  const cards = [];
  for (const word of parsed) {
    if (existing.has(normalize(word)) || flashcards.length + cards.length >= 500) continue;
    existing.add(normalize(word));
    cards.push(makeCard(word, "", source));
  }
  if (!cards.length) return setStatus(flashcards.length >= 500 ? pc("bulkLimit") : pc("duplicate"), true);
  flashcards.unshift(...cards);
  saveCards(); event.currentTarget.reset();
  setStatus(pc("bulkAdded").replace("{count}",cards.length)); render();
  queueEnrichment(cards);
});
pq("card-search").addEventListener("input", event => { searchTerm = event.target.value; render(); });
pq("card-filter").addEventListener("change", event => { filterMode = event.target.value; render(); });
pq("start-practice").addEventListener("click", startPractice);
pq("review-five").addEventListener("click", () => startReview("five"));
pq("review-month").addEventListener("click", () => startReview("month"));
pq("practice-deck-list").addEventListener("click", event => { const button = event.target.closest("[data-start-deck]"); if (button) startDeck(button.dataset.startDeck); });
pq("card-list").addEventListener("click", event => {
  const learn = event.target.closest("[data-card-learn]");
  const edit = event.target.closest("[data-card-edit]");
  const remove = event.target.closest("[data-card-delete]");
  if (edit) {
    const form = event.target.closest(".flashcard-row").querySelector("[data-card-edit-form]");
    form.hidden = !form.hidden;
    if (!form.hidden) form.querySelector("input").focus();
  }
  if (learn) {
    const card = flashcards.find(item => item.id === learn.dataset.cardLearn);
    if (card) {
      card.learned = !card.learned;
      card.srs = core.initialSrs(card.learned, card.createdAt);
      if (card.learned) card.srs.dueAt = Date.now() + 30 * core.DAY;
      saveCards(); render();
    }
  }
  if (remove) { flashcards = flashcards.filter(card => card.id !== remove.dataset.cardDelete); saveCards(); setStatus(pc("deleted")); render(); }
});
pq("card-list").addEventListener("submit", event => {
  const form = event.target.closest("[data-card-edit-form]");
  if (!form) return;
  event.preventDefault();
  const card = flashcards.find(item => item.id === form.dataset.cardEditForm);
  const translation = form.querySelector("input").value.trim();
  if (!card || !translation) return;
  card.translations[practiceLanguage] = translation.slice(0,240);
  card.translation = translation.slice(0,240);
  saveCards(); render();
});
pq("session-reveal").addEventListener("click", () => {
  const card = currentSessionCard();
  if (!card) return;
  meaningRevealed = true;
  pq("session-reveal").hidden = true;
  pq("session-meaning").hidden = false;
  pq("session-actions").hidden = false;
  renderMeaning(card);
  queueEnrichment([card], true);
});
pq("session-sentence").addEventListener("input", () => {
  const card = currentSessionCard();
  if (!card || pq("session-sentence").value.trim() === card.sentence) return;
  card.sentenceStatus = "idle";
  pq("sentence-feedback").textContent = "";
  pq("sentence-corrected").hidden = true;
  pq("sentence-translation").hidden = true;
});
pq("sentence-form").addEventListener("submit", async event => {
  event.preventDefault();
  const card = currentSessionCard();
  const sentence = pq("session-sentence").value.trim();
  if (!card || !sentence) return;
  if (!core.sentenceUsesWord(sentence, card.word)) { pq("sentence-feedback").textContent = pc("sentenceNeedWord"); pq("sentence-feedback").className = "sentence-feedback is-error"; return; }
  const button = pq("session-check");
  button.disabled = true;
  pq("sentence-feedback").textContent = pc("checkingSentence");
  pq("sentence-feedback").className = "sentence-feedback is-checking";
  card.sentence = sentence;
  card.sentenceStatus = "idle";
  if (!activeDeckId) saveCards();
  try {
    const result = await checkGrammar(sentence);
    if (card !== currentSessionCard()) return;
    const matches = result.matches || [];
    card.sentenceCorrected = correctedText(sentence, matches);
    card.sentenceStatus = matches.length ? "needs-review" : "correct";
    if (!activeDeckId) saveCards();
    pq("sentence-feedback").textContent = [
      pc("grammarLabel") + ": " + (matches.length ? "⚠ " + pc("grammarIssues") : "✓ " + pc("grammarOkay")),
      pc("meaningLabel") + ": ⚠ " + pc("formFound"),
      pc("naturalLabel") + ": ⚠ " + pc("notVerified")
    ].join("\n");
    pq("sentence-feedback").className = "sentence-feedback" + (matches.length ? " is-error" : " is-correct");
    pq("sentence-corrected").hidden = !matches.length || !card.sentenceCorrected || card.sentenceCorrected === sentence;
    if (!pq("sentence-corrected").hidden) pq("sentence-corrected").innerHTML = "<small>" + escapeHtml(pc("correctedLabel")) + "</small><strong>" + escapeHtml(card.sentenceCorrected) + "</strong>";
    if (!activeDeckId) render();
    refreshSessionTranslation();
  } catch {
    card.sentenceStatus = "needs-review";
    if (!activeDeckId) saveCards();
    pq("sentence-feedback").textContent = pc("sentenceServiceError");
    pq("sentence-feedback").className = "sentence-feedback is-error";
  } finally { button.disabled = false; }
});
pq("session-actions").addEventListener("click", event => { const button = event.target.closest("[data-rate]"); if (button) nextSession(button.dataset.rate); });
document.querySelectorAll("[data-practice-close]").forEach(element => element.addEventListener("click", closePractice));
document.addEventListener("keydown", event => {
  if (event.key === "Escape") { closePractice(); return; }
  if (!pq("practice-modal").classList.contains("is-open") || /^(INPUT|TEXTAREA|SELECT)$/.test(event.target.tagName)) return;
  if (!meaningRevealed && (event.key === "Enter" || event.key === " ")) { event.preventDefault(); pq("session-reveal").click(); }
  const rating = {"1":"again","2":"hard","3":"good","4":"easy"}[event.key];
  if (meaningRevealed && rating) nextSession(rating);
});
document.querySelectorAll("[data-practice-lang]").forEach(button => button.addEventListener("click", () => { practiceLanguage = button.dataset.practiceLang; history.replaceState({}, "", `practice.html?lang=${practiceLanguage}`); renderLanguage(); if (currentSessionCard()) { updateSession(); if (currentSessionCard().sentenceStatus === "correct") refreshSessionTranslation(); } }));
window.addEventListener("fullride:cloud-data", () => { flashcards = loadCards(); learnedDeckWords = loadDeckProgress(); renderDecks(); render(); queueEnrichment(flashcards.slice(0,120)); if (currentSessionCard()) updateSession(); });
renderLanguage();
